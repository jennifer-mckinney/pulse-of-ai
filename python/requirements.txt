# Embedding service dependencies
# Install: pip install -r requirements.txt

# Core embedding service
sentence-transformers==2.7.0
# Infinity (PyPI package: infinity-emb) is the planned Phase-2 embedding server
# per docs/TECHNICAL_SPEC.md §5 — add the dependency when actually adopted.

# Database
psycopg2-binary>=2.9.9
pgvector>=0.2.4

# HTTP server (used if running without Infinity)
fastapi>=0.110.0
uvicorn>=0.27.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
httpx>=0.26.0         # async HTTP client for test requests

# Code quality
black>=24.0.0         # code formatter (run: black python/)
flake8>=7.0.0         # linter
